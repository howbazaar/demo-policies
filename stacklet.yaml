collections:
  - name: "demo-time"
    schedule: "rate(10 minutes)"
    accounts:
      # This is the only account that will work for now.
      - 434520683652
    regions:
      - us-east-2
    policies:
      - tims-demo-security-group
      - tims-demo-s3
      - demo-skipped
